pipeline {
    /* insert Declarative Pipeline here */
    agent any
    stages {
        stage('Example Build') {
            steps {
                sh 'pdw'
                sh 'ls -lart'
            }
        }
    }
}