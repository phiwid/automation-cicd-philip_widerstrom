pipeline {
    agent any
    stages {
        stage('Deploy/Build app') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        
         stage('Frontend tests') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        
         stage('Backend tests') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        
             stage('Performance test') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
    }
}